using System.Collections;
using System.Collections.Generic;
using Tiles.Views;
using UnityEngine;

public class OpenSafes : MonoBehaviour
{
    private DominioViews dominoViews;
    private SafeManager safeManager;
    
   
    
    /*void Update()
    {
        if (DominoPos == tilePos[safepos]) 
        {
            rollbutton.SetActive(true);
            safeManager.OpenSafe();
            
            Could also use tags to determine whether a safe has been landed on 
            
        }
    }*/
}
